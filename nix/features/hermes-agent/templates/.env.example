# Copy to $PWD/.hermes/.env (never commit). devenv seeds this file once.
# Terminal backend: devenv sets TERMINAL_ENV=local by default; use hermes-sandbox for Docker.
# Create a key at https://cheapestinference.com/keys
CHEAPESTINFERENCE_API_KEY=
# OMNIROUTE_API_KEY=
# OPENROUTER_API_KEY=
