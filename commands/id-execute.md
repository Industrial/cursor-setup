# /id-execute

`[ID:EXECUTE]`

Enter **EXECUTE** only if plan approved (or user said plan+implement). Load `modes/execute.md`. Maestro claim (`--skip-worktree`, current branch)→contract→implement→evidence.

Pack root: `.cursor/commands/id-workflow/`.
